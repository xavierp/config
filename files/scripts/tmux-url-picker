#!/usr/bin/env bash
urls=$(tmux capture-pane -J -p | grep -oE 'https?://[^ >)]+' | sort -u)

if [[ -z "$urls" ]]; then
  tmux display-message "No URLs found"
  exit 0
fi

echo "$urls" | fzf-tmux -p 80%,40% --multi --bind 'enter:execute-silent(open {})+abort'
